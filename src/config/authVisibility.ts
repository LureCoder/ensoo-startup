// Authentication buttons visibility configuration

interface AuthVisibilityConfig {
  signIn: boolean;
  signUp: boolean;
}

const authVisibilityConfig: AuthVisibilityConfig = {
  signIn: false,
  signUp: false
};

export default authVisibilityConfig;
